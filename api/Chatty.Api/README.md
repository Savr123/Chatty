# Chatty - Real-time Chat Application

[![.NET](https://img.shields.io/badge/.NET-8.0-blue)](https://dotnet.microsoft.com/)
[![React](https://img.shields.io/badge/React-18.3.1-blue)](https://reactjs.org/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue)](https://www.postgresql.org/)
[![SignalR](https://img.shields.io/badge/SignalR-8.0.7-green)](https://dotnet.microsoft.com/apps/aspnet/signalr)
[![Docker](https://img.shields.io/badge/Docker-Ready-blue)](https://www.docker.com/)
[![License](https://img.shields.io/badge/License-MIT-green)](LICENSE)

Веб-приложение для обмена сообщениями в реальном времени с поддержкой аутентификации, групповых чатов и голосового общения.

## 📋 Оглавление

- [Обзор проекта](#обзор-проекта)
- [Архитектура](#архитектура)
- [Технологии](#технологии)
- [Системные требования](#системные-требования)
- [Установка и запуск](#установка-и-запуск)
- [API документация](#api-документация)
- [База данных](#база-данных)
- [Развертывание](#развертывание)
- [Разработка](#разработка)
- [Тестирование](#тестирование)
- [Вклад в проект](#вклад-в-проект)
- [Лицензия](#лицензия)

## 🎯 Обзор проекта

**Chatty** - это полнофункциональное приложение для обмена сообщениями в реальном времени, состоящее из:

- **Backend API** (.NET 8) - REST API с SignalR для real-time коммуникации
- **Frontend Client** (React) - Современный веб-интерфейс с Material-UI
- **Database** (PostgreSQL) - Реляционная база данных для хранения пользователей и сообщений
- **Docker** - Контейнеризация для простого развертывания

### Основные возможности

- ✅ Аутентификация пользователей (JWT)
- ✅ Регистрация и авторизация
- ✅ Real-time обмен сообщениями (SignalR)
- ✅ Групповые чаты
- ✅ Современный responsive UI (Material-UI)
- ✅ Docker контейнеризация
- ✅ Swagger API документация
- ✅ Entity Framework Code-First миграции

## 🏗️ Архитектура

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   React Client  │    │   .NET API      │    │   PostgreSQL    │
│   (Port 3000)   │◄──►│   (Port 5000)   │◄──►│   (Port 5433)   │
│                 │    │                 │    │                 │
│ - Material-UI   │    │ - ASP.NET Core  │    │ - Users         │
│ - Redux         │    │ - SignalR       │    │ - Chats         │
│ - SignalR Client│    │ - JWT Auth      │    │ - Messages      │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### Backend структура

```
api/Chatty.Api/
├── Controllers/          # API контроллеры
│   ├── ChatController.cs    # Управление чатами
│   └── UserController.cs    # Управление пользователями
├── Models/               # Модели данных
│   ├── User.cs             # Модель пользователя
│   ├── Chat.cs             # Модель чата
│   └── Message.cs          # Модель сообщения
├── Services/             # Бизнес-логика
│   ├── IUserService.cs     # Интерфейс сервиса пользователей
│   └── UserService.cs      # Реализация сервиса пользователей
├── Hubs/                 # SignalR хабы
│   ├── ChatHub.cs          # Real-time чат
│   └── Clients/IChatClient.cs
├── Helpers/              # Вспомогательные классы
│   ├── AuthOptions.cs      # Настройки аутентификации
│   ├── AutoMapperProfile.cs # Профиль маппинга
│   └── AppSettings.cs      # Настройки приложения
└── Migrations/           # EF миграции
```

### Frontend структура

```
client/
├── src/
│   ├── Components/         # React компоненты
│   │   ├── Account/        # Аутентификация
│   │   ├── Chat/           # Компоненты чата
│   │   └── NavBar.js       # Навигация
│   ├── Redux/              # Управление состоянием
│   │   ├── Slices/         # Redux slices
│   │   └── store.js        # Redux store
│   └── Services/           # Сервисы
└── public/                 # Статические файлы
```

## 🛠️ Технологии

### Backend (.NET 8)

| Компонент | Версия | Назначение |
|-----------|--------|------------|
| ASP.NET Core | 8.0 | Веб-фреймворк |
| Entity Framework Core | 9.0.6 | ORM для работы с БД |
| Npgsql.EntityFrameworkCore.PostgreSQL | 9.0.4 | PostgreSQL провайдер для EF |
| Microsoft.AspNetCore.Authentication.JwtBearer | 6.0.4 | JWT аутентификация |
| Microsoft.AspNetCore.SignalR | 8.0 | Real-time коммуникация |
| AutoMapper | 12.0.1 | Маппинг объектов |
| Swashbuckle.AspNetCore | 6.2.3 | Swagger документация |

### Frontend (React)

| Компонент | Версия | Назначение |
|-----------|--------|------------|
| React | 18.3.1 | UI фреймворк |
| @microsoft/signalr | 8.0.7 | SignalR клиент |
| @mui/material | 6.1.4 | UI компоненты |
| @reduxjs/toolkit | 2.3.0 | Управление состоянием |
| react-router-dom | 6.27.0 | Маршрутизация |
| redux-persist | 6.0.0 | Персистентное состояние |

### Infrastructure

| Компонент | Версия | Назначение |
|-----------|--------|------------|
| PostgreSQL | 15-alpine | База данных |
| Docker | 20+ | Контейнеризация |
| Docker Compose | 2.0+ | Оркестрация контейнеров |

## 💻 Системные требования

### Минимальные требования

- **ОС**: Windows 10 / 11, Linux (Ubuntu 18.04+), macOS (10.15+)
- **RAM**: 4 GB
- **Диск**: 2 GB свободного места
- **Node.js**: 16.x или выше
- **.NET SDK**: 8.0 или выше
- **Docker**: 20.0 или выше (опционально)

### Рекомендуемые требования

- **ОС**: Windows 11, Linux (Ubuntu 20.04+), macOS 12+
- **RAM**: 8 GB
- **Диск**: 5 GB свободного места
- **Node.js**: 18.x LTS
- **.NET SDK**: 8.0 LTS
- **PostgreSQL**: 15.x (или Docker)
- **Docker Desktop**: Последняя версия

## 🚀 Установка и запуск

### Способ 1: Docker (Рекомендуемый)

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd Chatty

# 2. Запустить через Docker Compose
docker-compose up --build

# 3. Приложение будет доступно по адресам:
# Frontend: http://localhost:3000
# API: http://localhost:5000
# Swagger: http://localhost:5000/swagger
# PostgreSQL: localhost:5433
```

### Способ 2: Локальная разработка

#### Backend (.NET API)

```bash
# Перейти в директорию API
cd api/Chatty.Api

# Восстановить зависимости
dotnet restore

# Запустить миграции (если нужно)
dotnet ef database update

# Запустить приложение
dotnet run
# Или использовать bat-файл
run.bat
```

#### Frontend (React)

```bash
# Перейти в директорию клиента
cd client

# Установить зависимости
npm install

# Запустить в режиме разработки
npm start
```

#### База данных (PostgreSQL)

```bash
# Через Docker (рекомендуемый способ)
docker run --name chatty-postgres -e POSTGRES_DB=ChattyDb -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=admin -p 5433:5432 -d postgres:15-alpine

# Или установить PostgreSQL локально и создать базу данных
createdb ChattyDb
```

### Способ 3: IDE запуск

```bash
# Открыть решение в Visual Studio 2022+
# или VS Code с C# Dev Kit расширением

# Запустить проект Chatty.Api
# Frontend запустить отдельно через npm start
```

## 📚 API документация

### Базовый URL
```
http://localhost:5000
```

### Аутентификация

Приложение использует JWT токены для аутентификации. Получить токен можно через endpoint `/signIn/{username}` или через `/User/Login`.

### Основные endpoints

#### Пользователи

| Метод | Endpoint | Описание | Авторизация |
|-------|----------|----------|-------------|
| POST | `/User/Registration` | Регистрация пользователя | Нет |
| POST | `/User/Login` | Авторизация пользователя | Нет |
| POST | `/User/Authenticate` | Аутентификация (альтернативный) | Нет |

#### Чаты

| Метод | Endpoint | Описание | Авторизация |
|-------|----------|----------|-------------|
| POST | `/Chat/messages` | Отправить сообщение | JWT |

#### SignalR хабы

| Хаб | Endpoint | Описание |
|-----|----------|----------|
| ChatHub | `/hubs/chat` | Real-time чат |

### Примеры запросов

#### Регистрация пользователя
```bash
POST /User/Registration
Content-Type: application/json

{
  "firstName": "Иван",
  "lastName": "Иванов",
  "email": "ivan@example.com",
  "password": "password123"
}
```

#### Авторизация
```bash
POST /User/Login
Content-Type: application/json

{
  "email": "ivan@example.com",
  "password": "password123"
}
```

#### Отправка сообщения через SignalR
```javascript
const connection = new signalR.HubConnectionBuilder()
    .withUrl('/hubs/chat')
    .build();

await connection.start();

await connection.invoke('SendMessage', {
    id: 'msg-123',
    text: 'Привет всем!',
    date: new Date().toISOString()
});
```

## 🗄️ База данных

### Схема базы данных

```sql
-- Пользователи
CREATE TABLE "Users" (
    "Id" SERIAL PRIMARY KEY,
    "firstName" VARCHAR(255) NOT NULL,
    "lastName" VARCHAR(255) NOT NULL,
    "username" VARCHAR(255) UNIQUE NOT NULL,
    "email" VARCHAR(255) UNIQUE NOT NULL,
    "PasswordHash" BYTEA NOT NULL,
    "PasswordSalt" BYTEA NOT NULL,
    "registrationDate" TIMESTAMP NOT NULL
);

-- Чаты
CREATE TABLE "Chats" (
    "Id" SERIAL PRIMARY KEY,
    "name" VARCHAR(255) DEFAULT 'Unknown'
);

-- Сообщения
CREATE TABLE "Messages" (
    "Id" VARCHAR(255) PRIMARY KEY,
    "Text" TEXT,
    "Date" TIMESTAMP NOT NULL,
    "ChatsId" INTEGER REFERENCES "Chats"("Id"),
    "UsersId" INTEGER REFERENCES "Users"("Id")
);

-- Связь пользователей и чатов (Many-to-Many)
CREATE TABLE "UserChatList" (
    "UsersId" INTEGER REFERENCES "Users"("Id"),
    "ChatsId" INTEGER REFERENCES "Chats"("Id"),
    PRIMARY KEY ("UsersId", "ChatsId")
);
```

### Entity Framework миграции

```bash
# Создать новую миграцию
dotnet ef migrations add MigrationName

# Применить миграции
dotnet ef database update

# Откатить миграцию
dotnet ef database update PreviousMigrationName
```

## 🐳 Развертывание

### Docker Compose

Основной файл `docker-compose.yml` содержит конфигурацию для:

- **PostgreSQL** (порт 5433)
- **API** (порт 5000)
- **React Client** (порт 3000 для продакшена, 3001 для разработки)

```bash
# Сборка и запуск
docker-compose up --build

# Запуск в фоне
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f
```

### Production развертывание

```bash
# Сборка для продакшена
docker-compose -f docker-compose.yml up --build

# Использование nginx для проксирования
# (конфигурация в nginx/ директории)
```

### Переменные окружения

#### API (.NET)
```env
ASPNETCORE_ENVIRONMENT=Production
ConnectionStrings__PostgreSQLConnectionString=Host=postgres;Port=5432;Database=ChattyDb;Username=postgres;Password=admin;
ASPNETCORE_URLS=http://+:80
```

#### React Client
```env
REACT_APP_API_URL=http://localhost:5000
```

## 💻 Разработка

### Настройка среды разработки

1. **Установить .NET 8 SDK**
2. **Установить Node.js 18+**
3. **Установить PostgreSQL** или использовать Docker
4. **Клонировать репозиторий**
5. **Запустить базу данных**
6. **Запустить API**: `cd api/Chatty.Api && dotnet run`
7. **Запустить клиент**: `cd client && npm start`

### Code Style

#### C# (Backend)
- Использовать PascalCase для имен классов и методов
- camelCase для параметров и локальных переменных
- Следовать принципам SOLID
- Использовать async/await для асинхронных операций

#### JavaScript/React (Frontend)
- Использовать стрелочные функции
- PascalCase для компонентов
- camelCase для переменных и функций
- Использовать функциональные компоненты с хуками

### Добавление новых функций

1. **Backend**: Создать контроллер/сервис/модель
2. **Database**: Добавить миграцию EF
3. **Frontend**: Создать компонент и обновить Redux store
4. **API**: Обновить SignalR хабы при необходимости

### Отладка

```bash
# Backend логи
# Логи выводятся в консоль при запуске dotnet run

# Frontend логи
# Открыть DevTools браузера (F12)

# SignalR отладка
# В appsettings.json установить уровень логирования SignalR
"Microsoft.AspNetCore.SignalR": "Debug"
```

## 🧪 Тестирование

### Backend тесты

```bash
cd api/Chatty.Api
dotnet test
```

### Frontend тесты

```bash
cd client
npm test
```

### API тестирование

- **Swagger UI**: http://localhost:5000/swagger
- **Postman**: Импортировать коллекцию API
- **cURL**: Использовать для ручного тестирования

## 🤝 Вклад в проект

1. Fork репозиторий
2. Создать feature branch (`git checkout -b feature/AmazingFeature`)
3. Зафиксировать изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Открыть Pull Request

### Требования к PR

- [ ] Код следует code style проекта
- [ ] Добавлены тесты для новых функций
- [ ] Обновлена документация
- [ ] Все тесты проходят
- [ ] PR описан подробно

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🙏 Благодарности

- [ASP.NET Core](https://docs.microsoft.com/en-us/aspnet/core/) - Веб-фреймворк
- [React](https://reactjs.org/) - UI библиотека
- [Material-UI](https://mui.com/) - Компоненты интерфейса
- [SignalR](https://dotnet.microsoft.com/apps/aspnet/signalr) - Real-time коммуникация
- [Entity Framework](https://docs.microsoft.com/en-us/ef/) - ORM

## 📞 Контакты

- **Email**: ваш-email@example.com
- **GitHub**: [ваш-username](https://github.com/ваш-username)
- **Issues**: [GitHub Issues](https://github.com/ваш-username/chatty/issues)

---

⭐ Если проект вам понравился, поставьте звезду на GitHub!
