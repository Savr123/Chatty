# Быстрый запуск React приложения Chatty

## 🚀 САМЫЙ ПРОСТОЙ СПОСОБ (рекомендуется)

### Просто запустите файл `start.bat`
1. Откройте папку `D:\Repos\PetProjects\Chatty\client`
2. Дважды кликните на файл **`start.bat`**
3. Готово! Приложение запустится автоматически

### Или используйте PowerShell скрипт `start.ps1`
1. Откройте PowerShell в папке проекта
2. Выполните: `.\start.ps1`

---

## 📋 Ручной запуск (если нужен контроль)

### 1. Откройте командную строку (CMD, не PowerShell!)

### 2. Перейдите в папку проекта
```bash
cd "D:\Repos\PetProjects\Chatty\client"
```

### 3. Установите переменные окружения
```bash
set OPENSSL_CONF=.\openssl.cnf
set REACT_APP_SERVER_HTTP_ROOT=http://localhost:8000/api
```

### 4. Запустите приложение
```bash
npm start
```

## Возможные проблемы и решения

### Проблема: "OpenSSL configuration error"
**Решение**: Убедитесь, что файл `openssl.cnf` находится в корне проекта и выполните:
```bash
set OPENSSL_CONF=.\openssl.cnf
```

### Проблема: "Port 3000 is already in use"
**Решение**: Остановите другие процессы на порту 3000:
```bash
netstat -ano | findstr :3000
taskkill /F /PID [номер_процесса]
```

### Проблема: "Command not found: npm"
**Решение**: Установите Node.js с официального сайта: https://nodejs.org/

### Проблема: "Module not found"
**Решение**: Установите зависимости:
```bash
npm install
```

## Проверка успешного запуска

После выполнения `npm start` вы должны увидеть:
```
Compiled successfully!

You can now view clientreact in the browser.

  Local:            http://localhost:3000
  On Your Network:  http://172.20.224.1:3000
```

## Альтернативный способ запуска

Если `npm start` не работает, попробуйте:
```bash
npx react-scripts start
```

## Остановка приложения

Для остановки нажмите `Ctrl+C` в командной строке.
